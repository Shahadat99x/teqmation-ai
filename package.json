{
  "name": "business-automation",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @clientflow-ai/web",
    "build:web": "npm run build --workspace @clientflow-ai/web",
    "typecheck:web": "npm run typecheck --workspace @clientflow-ai/web"
  }
}

